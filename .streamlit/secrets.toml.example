# Copy this file's contents into your Streamlit Community Cloud app's
# "Secrets" settings (App settings -> Secrets), NOT into a committed
# secrets.toml - this file is only a template (the real one is
# gitignored). Streamlit Cloud injects these as environment variables,
# which is what frontend/app.py reads via os.environ.

RETURNGUARD_API_URL = "https://returnguard-backend.onrender.com"
